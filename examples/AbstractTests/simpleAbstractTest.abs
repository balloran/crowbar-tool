module Simple;

["ae_specvars locset footprintQ, frameQ, relevantLocs, testQ"]
["ae_constraint disjoint(footprintQ, frameQ, testQ)"]
["normal_behavior requires (x >= y)"]

{

    Int x = 2;
    x = x + 1;
    ["accessible footprintQ"]
    ["assignable hasTo(x)"]
    ["abstract_statement Q"]
    Int y = 0;
    x = 4;
    x = 5;
    skip;
}